name: CI

on:
  push:
    branches: ["main", "release/*"]
    paths:
      - "**.rs"
      - "**.py"
      - "**/Cargo.toml"
      - "**/Cargo.lock"
      - "**/pyproject.toml"
      - "**/poetry.lock"
  pull_request:
    branches: ["main", "release/*"]
    paths:
      - "**.rs"
      - "**.py"
      - "**/Cargo.toml"
      - "**/Cargo.lock"
      - "**/pyproject.toml"
      - "**/poetry.lock"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # Static analysis is done only on ubuntu to speedup the process
  static-analysis:
    name: "Static analysis"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable
          components: rustfmt, clippy
      - name: Rust Cache
        uses: Swatinem/rust-cache@v2.8.2
        with:
          workspaces: "mosaicod -> target"
      
      - name: Backend Lint
        working-directory: mosaicod
        run: |
          cargo fmt -- --check
          cargo clippy -- -D warnings

      - name: Python SDK Lint
        run: |
          pipx install ruff
          ruff format --check mosaico-sdk-py
          ruff check mosaico-sdk-py

  backend-unit-tests:
    name: "mosaicod: unit tests on (${{ matrix.os }})"
    needs: [static-analysis]
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, ubuntu-24.04, macos-26]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable

      - name: Install System Dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libpq-dev

      - name: Install System Dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install libpq

      - name: Unit Tests
        run: ./scripts/tests.sh --mosaicod

  python-sdk-unit-tests:
    name: "Python SDK: unit tests on (${{ matrix.os }})"
    needs: [static-analysis]
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, ubuntu-24.04, macos-26]
    runs-on: ${{ matrix.os }}
    env:
      PYTHON_KEYRING_BACKEND: keyring.backends.null.Keyring
    steps:
      - uses: actions/checkout@v6
      - name: Install Poetry
        run: pipx install poetry
      - name: Setup Python
        uses: actions/setup-python@v6.1.0
        with:
          python-version: "3.13"
          cache: "poetry"
      - name: Unit Tests
        run: ./scripts/tests.sh --sdk-python

  full-stack-tests:
    name: "Full stack tests on (${{ matrix.os }})"
    needs: [backend-unit-tests, python-sdk-unit-tests]
    strategy:
      fail-fast: false
      matrix:
        os: [ubutntu-22.04, ubuntu-24.04, macos-26]
    runs-on: ${{ matrix.os }}
    env:
      PYTHON_KEYRING_BACKEND: keyring.backends.null.Keyring
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: stable
      
      - name: Install Dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libpq-dev
      
      - name: Install Dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install libpq

      - name: Install Poetry
        run: pipx install poetry
      - uses: actions/setup-python@v6.1.0
        with:
          python-version: "3.13"
          cache: "poetry"

      - name: Integration Tests
        run: |
          ./scripts/tests.sh --full-stack
          ./scripts/tests.sh --full-stack-tls
